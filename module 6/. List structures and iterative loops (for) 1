import random

rolls = int(input("How many times would you like to roll the dice?: "))
for _ in range(rolls):
    dice1 = random.randint(1, 6)
    dice2 = random.randint(1, 6)
    sum = dice1 + dice2
    print(f"Roll summary: {dice1} + {dice2} = {sum}")